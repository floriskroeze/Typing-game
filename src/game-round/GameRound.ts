import GameState from "../state/GameState.ts";

export default class GameRound {
    static instance: GameRound;

    constructor(_clock: any, _gameState: GameState, _settings: any, _gameText: any) {
    }

    startRound() {

    }

    endRound() {

    }
}